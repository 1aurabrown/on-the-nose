<div class="">
  <h1>{{ collection.title }}</h1>
  {% if collection.description != blank %}
    {{ collection.description }}
  {% endif %}
</div>

{% schema %}
{
  "name": "Collection Header",
  "tag": "section",
  "enabled_on": {
    "templates": [
      "collection"
    ]
  }
}
{% endschema %}
