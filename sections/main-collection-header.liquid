<div class="padding-x max-w-screen-2xl mx-auto text-link-container padding-top pb-15 md:pb-13 border-b rte">
  {% if collection.description != blank %}
    {{ collection.description }}
  {% endif %}
</div>

{% schema %}
{
  "name": "Collection Header",
  "tag": "section",
  "enabled_on": {
    "templates": [
      "collection"
    ]
  }
}
{% endschema %}
